apiVersion: apps/v1
kind: Deployment
metadata:
  name: amd-gpu
  labels:
    app: ollama
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
      provider: amd-gpu
  template:
    metadata:
      labels:
        app: ollama
        provider: amd-gpu
    spec:
      containers:
        - name: ollama
          image: ollama/ollama:0.1.47
          ports:
            - containerPort: 11434
          securityContext:
            privileged: true
