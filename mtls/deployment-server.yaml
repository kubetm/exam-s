apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  namespace: mtls
spec:
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      containers:
        - name: server
          image: 1pro/exam
          ports:
            - containerPort: 8080